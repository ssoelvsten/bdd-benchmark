#ifndef BDD_BENCHMARK_BASIC_ADAPTER_H
#define BDD_BENCHMARK_BASIC_ADAPTER_H

#include "common/chrono.h"
#include "common/input.h"
#include "common/json.h"

#include <exception>
#include <functional>
#include <string>
#include <string_view>
#include <vector>
#include <utility>

////////////////////////////////////////////////////////////////////////////////////////////////////
/// \brief Integer logarithm floor(log2(n))
///
/// \param n Must not be 0
////////////////////////////////////////////////////////////////////////////////////////////////////
constexpr unsigned
ilog2(unsigned long long n)
{
  assert(n > 0);

#ifdef __GNUC__
  // GCC and Clang support `__builtin_clzll`
  //   "clz" stands for count leading zero bits. The builtin function may be implemented more
  //    efficiently than the loop below.
  return sizeof(unsigned long long) * 8 - __builtin_clzll(n) - 1;
#else
  unsigned exp           = 1u;
  unsigned long long val = 2u; // 2^1
  while (val < n) {
    val <<= 1u;
    exp++;
  }

  return exp;
#endif
}

////////////////////////////////////////////////////////////////////////////////////////////////////
/// \brief Exception thrown by a BDD package if it does not support the given BDD operation.
////////////////////////////////////////////////////////////////////////////////////////////////////
class unsupported_operation
  : public std::logic_error
{
public:
  unsupported_operation(const std::string msg)
    : std::logic_error(msg)
  {}
};

////////////////////////////////////////////////////////////////////////////////////////////////////
/// \brief Common super class for all BDD package adapters.
///
/// \details This common interface is achieved via *virtual* functions. Yet, at compile time the
///          exact type is known. Hence, the compiler should be able to fully resolve the overload
///          *early*. To guarantee this is fully inlined, we can switch over to using the *curiously
///          recurring template pattern*.
////////////////////////////////////////////////////////////////////////////////////////////////////
template <typename Constants, typename DdType, typename BuildNodeType = DdType>
class basic_adapter
{
  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Exceptions
protected:

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // \brief Throws an `unsupported_operation` with the given operation name.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  inline unsupported_operation
  unsupported(const std::string op)
  {
    return unsupported_operation(std::string(Constants::name) + " does not support '" + op + "'");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Types
public:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Type of decision diagram.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  using dd_t = DdType;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Type of pointers to diagram nodes during node-by-node construction.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  using build_node_t = BuildNodeType;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Variables
protected:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Amount of memory (in bytes) available to the BDD package.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  const size_t _memory;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Number of decision diagram variables.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  const int _varcount;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Constants
public:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Name of the BDD package.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  static constexpr std::string_view name = Constants::name;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Type of the decision diagram.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  static constexpr std::string_view dd = Constants::dd;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Whether this BDD package adapter needs to use `extend` on its decision diagrams to
  ///        account for variables currently outside the diagram's domain.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  static constexpr bool needs_extend = Constants::needs_extend;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Whether this BDD package needs the frame rule to be explicitly encoded for its
  ///        relational product, i.e. `relnext` and `relprev`.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  static constexpr bool needs_frame_rule = Constants::needs_frame_rule;

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Init- and Deinitialization
public:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Initialises the BDD package with `vc` number of variables.
  ///
  /// \param vc Number of decision diagram variables. This value cannot be extended later.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  basic_adapter(int vc)
    : _memory(static_cast<size_t>(M) * 1024u * 1024u)
    , _varcount(vc)
  {}

  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual ~basic_adapter()
  {}

public:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Runs function `f` in the correct context for the BDD package.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  template <typename F>
  int
  run(const F& f)
  {
    return f();
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // DD Basic Constructors
public:

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The decision diagram for the `true` formula.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  top()
  {
    throw unsupported("top()");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The decision diagram for the `false` formula.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  bot()
  {
    throw unsupported("bot()");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The decision diagram for single variable `i` being `true`.
  ///
  /// \TODO Rename to `var(i)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  ithvar(int)
  {
    throw unsupported("ithvar(i)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The decision diagram for single variable `i` being `false`.
  ///
  /// \TODO Rename to `nvar(i)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  nithvar(int)
  {
    throw unsupported("nithvar(i)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The cube of variables (bottom-up) from `rbegin` to `rend`.
  ///
  /// \TODO Rename to `mk_cube(f, rbegin, rend)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  template <typename IT>
  dd_t
  cube(IT rbegin, IT rend)
  {
    dd_t res = top();
    while (rbegin != rend) { res = ite(ithvar(*(rbegin++)), res, bot()); }
    return res;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief The cube of variables (bottom-up) for which `pred` is true.
  ///
  /// \TODO Rename to `mk_cube(f, pred)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  cube(const std::function<bool(int)>& pred)
  {
    dd_t res = top();
    for (int i = _varcount - 1; 0 <= i; --i) {
      if (pred(i)) { res = ite(ithvar(i), res, bot()); }
    }
    return res;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // DD Manipulation
public:

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the negation of `f`.
  ///
  /// \TODO Rename to `mk_not(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  neg(const dd_t& /*f*/)
  {
    throw unsupported("neg(f)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the conjunction of `f` and `g`.
  ///
  /// \TODO Rename to `mk_and(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_and(const dd_t& /*f*/, const dd_t& /*g*/)
  {
    throw unsupported("apply_and(f, g)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the disjunction of `f` and `g`.
  ///
  /// \TODO Rename to `mk_or(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_or(const dd_t& f, const dd_t& g)
  {
    throw neg(apply_and(neg(f), neg(g)));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the (set) difference of `f` and `g`.
  ///
  /// \TODO Rename to `mk_diff(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_diff(const dd_t& f, const dd_t& g)
  {
    return apply_and(f, neg(g));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the logical implication from `f` to `g`.
  ///
  /// \TODO Rename to `mk_imp(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_imp(const dd_t& f, const dd_t& g)
  {
    return apply_or(neg(f), g);
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the exclusive or of `f` and `g`.
  ///
  /// \TODO Rename to `mk_xor(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_xor(const dd_t& f, const dd_t& g)
  {
    throw apply_or(apply_and(f, neg(g)), apply_and(neg(f), g));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the negated exclusive or of `f` and `g`.
  ///
  /// \TODO Rename to `mk_xnor(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  apply_xnor(const dd_t& f, const dd_t& g)
  {
    return neg(apply_xor(f, g));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes if `f` then `g` else `h`.
  ///
  /// \TODO Rename to `mk_ite(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  ite(const dd_t& f, const dd_t& g, const dd_t& h)
  {
    return apply_or(apply_and(f, g), apply_and(neg(f), h));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Restricts variable `i` to the boolean value `v`.
  ///
  /// \TODO Rename to `mk_restrict(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  restrict(const dd_t& /*f*/, const int /*i*/, const bool /*v*/)
  {
    throw unsupported("restrict(f, i, v)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Extends `f` to also accomodate the (don't care) variables from `begin` to `end`.
  ///
  /// \TODO Rename to `mk_extend(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  template <typename IT>
  dd_t
  extend(const dd_t& f, IT /*begin*/, IT /*end*/)
  {
    if constexpr (Constants::needs_extend) {
      throw unsupported("extend(f, begin, end)");
    } else {
      return f;
    }
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Existentially quantifies variable `i`.
  ///
  /// \TODO Rename to `mk_exists(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  exists(const dd_t& f, int i)
  {
    return apply_or(restrict(f, i, false), restrict(f, i, true));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Existentially quantifies all variables for which `pred` is returns true.
  ///
  /// \TODO Rename to `mk_exists(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  exists(const dd_t& f, const std::function<bool(int)>& pred)
  {
    dd_t g = f;
    for (int i = 0; i < _varcount; ++i) {
      if (!pred(i)) { continue; }
      g = exists(g, i);
    }
    return g;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Existentially quantifies all variables between `rbegin` and `rend`.
  ///
  /// \TODO Rename to `mk_exists(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  template <typename IT>
  dd_t
  exists(const dd_t& f, IT rbegin, IT rend)
  {
    dd_t g = f;
    for (; rbegin != rend; ++rbegin) {
      g = exists(g, *rbegin);
    }
    return g;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Universally quantifies variable `i`.
  ///
  /// \TODO Rename to `mk_forall(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  forall(const dd_t& f, int i)
  {
    return apply_and(restrict(f, i, false), restrict(f, i, true));
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Universally quantifies all variables for which `pred` is returns true.
  ///
  /// \TODO Rename to `mk_forall(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  forall(const dd_t& f, const std::function<bool(int)>& pred)
  {
    dd_t g = f;
    for (int i = 0; i < _varcount; ++i) {
      if (!pred(i)) { continue; }
      g = forall(g, i);
    }
    return g;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Universally quantifies all variables between `rbegin` and `rend`.
  ///
  /// \TODO Rename to `mk_forall(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  template <typename IT>
  dd_t
  forall(const dd_t& f, IT rbegin, IT rend)
  {
    dd_t g = f;
    for (; rbegin != rend; ++rbegin) {
      g = forall(g, *rbegin);
    }
    return g;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the relational product in the *forwards* direction.
  ///
  /// \details We use an interleaved variable ordering. That is, *current* state variables are even
  ///          while the *next* state variables are odd (directly after the corresponding *current*
  ///          variable).
  ///
  /// \details If `needs_frame_rule` is false, then `rel` skips explicitly preserving unchangsed
  ///          variables. Hence, the `rel_support` provides a cube of the variables that are
  ///          actually affected by `rel`.
  ///
  /// \TODO Rename to `mk_relnext(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  relnext(const dd_t& /*states*/, const dd_t& /*rel*/, const dd_t& /*rel_support*/)
  {
    throw unsupported("relnext(S, R, _)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Computes the relational product in the *backwards* direction.
  ///
  /// \details We use an interleaved variable ordering. That is, *current* state variables are even
  ///          while the *next* state variables are odd (directly after the corresponding *current*
  ///          variable).
  ///
  /// \details If `needs_frame_rule` is false, then `rel` skips explicitly preserving unchangsed
  ///          variables. Hence, the `rel_support` provides a cube of the variables that are
  ///          actually affected by `rel`.
  ///
  /// \TODO Rename to `mk_relnext(f)`
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  relprev(const dd_t& /*states*/, const dd_t& /*rel*/, const dd_t& /*rel_support*/)
  {
    throw unsupported("relprev(S, R, _)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // DD Queries
public:

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Number of decision diagram nodes nodes used to represent `f` (including terminals).
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual uint64_t
  nodecount(const dd_t& /*f*/)
  {
    throw unsupported("nodecount(f)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Number of satisfying assignments.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual uint64_t
  satcount(const dd_t& f)
  {
    return this->satcount(f, this->_varcount);
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Number of satisfying assignments for a given number of variables.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual uint64_t
  satcount(const dd_t& /*f*/, const size_t /*vc*/)
  {
    throw unsupported("satcount(f, vc)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Provides a decision diagram for some satisfying assignment.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  satone(const dd_t& f)
  {
    throw unsupported("satone(f)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Provides a decision diagram for some satisfying assignment, including all variables in
  ///        `c` no matter whether they are part of the support of `f`.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual dd_t
  satone(const dd_t& /*f*/, const dd_t& /*c*/)
  {
    throw unsupported("satone(f, c)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Extracts a satisfying assignment (cube) from `f` into a `std::vector` of `std::pair`s
  ///        of a variable (`int`) and its assignment (`char`). The assignment is `0` for *false*,
  ///        `1` for *true*, and `2` for *don't care*.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual std::vector<std::pair<int, char>>
  pickcube(const dd_t& /*f*/)
  {
    throw unsupported("pickcube(f)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Advanced DD Construction (Node-by-node building)
public:
  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Prints the decision diagram `f` as a graphviz `.dot` file to disk.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  void
  print_dot(const dd_t& f, const std::string& filename)
  {
    throw unsupported("print_dot(f, filename)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Saves the decision diagram on disk.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  void
  save(const dd_t& f, const std::string& filename)
  {
    throw unsupported("save(f, filename)");
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  // Statistics
public:

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Number of nodes allocated in the unique node table.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  virtual size_t
  allocated_nodes()
  {
    return 0;
  }

  //////////////////////////////////////////////////////////////////////////////////////////////////
  /// \brief Prints statistics specific specific to this BDD package.
  //////////////////////////////////////////////////////////////////////////////////////////////////
  void
  print_stats()
  {}
};

// TODO: basic_bdd_builder

////////////////////////////////////////////////////////////////////////////////////////////////////
/// \brief Initializes the BDD package adapter and runs the given benchmark.
////////////////////////////////////////////////////////////////////////////////////////////////////
template <typename Adapter, typename F>
int
run(const std::string& benchmark_name, const int varcount, const F& f)
{
  std::cout << json::brace_open << json::endl;

  std::cout
    // Debug mode
    << json::field("debug_mode")
#ifndef NDEBUG
    << json::value(true)
#else
    << json::value(false)
#endif
    << json::comma
    << json::endl
    // Statistics
    << json::field("statistics")
#ifdef BDD_BENCHMARK_STATS
    << json::value(true)
#else
    << json::value(false)
#endif
    << json::comma << json::endl
    << json::endl
    // BDD package substruct
    << json::field("bdd package") << json::brace_open
    << json::endl
    // Name
    << json::field("name") << json::value(Adapter::name) << json::comma
    << json::endl
    // BDD Type
    << json::field("type") << json::value(Adapter::dd) << json::comma << json::endl;

  const time_point t_before = now();
  Adapter adapter(varcount);
  const time_point t_after = now();

  const time_duration t_duration = duration_ms(t_before, t_after);
#ifdef BDD_BENCHMARK_INCL_INIT
  init_time = t_duration;
#endif // BDD_BENCHMARK_INCL_INIT

  std::cout
    // Initialisation Time
    << json::field("init time (ms)") << json::value(t_duration) << json::comma
    << json::endl
    // Memory
    << json::field("memory (MiB)") << json::value(M) << json::comma
    << json::endl
    // Variables
    << json::field("variables") << json::value(varcount)
    << json::endl
    // ...
    << json::brace_close << json::comma << json::endl
    << json::endl;

  std::cout << json::field("benchmark") << json::brace_open << json::endl;
  std::cout << json::field("name") << json::value(benchmark_name) << json::comma << json::endl
            << json::flush;

  const int exit_code = adapter.run([&]() { return f(adapter); });

  std::cout << json::brace_close << json::endl << json::brace_close << json::endl << json::flush;

#ifdef BDD_BENCHMARK_STATS
  if (!exit_code) { adapter.print_stats(); }
#endif

#ifdef BDD_BENCHMARK_WAIT
  // TODO: move to 'std::cerr' to keep 'std::cout' pure JSON?
  std::cout << "\npress any key to exit . . .\n" << std::flush;
  ;
  std::getchar();
  std::cout << "\n";
#endif

  return exit_code;
}

#endif // BDD_BENCHMARK_BASIC_ADAPTER_H
